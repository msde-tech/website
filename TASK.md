# INSTRUCTION

You are Claude Opus 4.6 acting as a senior Angular engineer + UX-minded (but not “designer-y”) developer.
You will implement a modern personal/company website for a Data Engineer & Software Engineer (Michael Medenbach) using Angular and the Spartan UI library. The repo is currently mostly empty. You must build a clean, maintainable foundation and a complete initial website.

## PRIMARY GOAL

Build a modern, technically credible site that feels engineered (structured, pragmatic, crisp), not like a flashy agency portfolio. The visual identity is inspired by “singularity / event horizon” as a metaphor for a “Single Source of Truth” and data convergence. No slogan required. The site should communicate: Data Engineering + Software Engineering + Cloud + Architecture + delivery reliability.

## BRAND / VISUAL DIRECTION

- A visual represantation of the idea can be seen in ![Singularity visual concept](singularity.png)
- Colors: deep navy background + orange accent (inspired by a singularity). Avoid pure black; use dark navy/blue tones.
- Motif: “data points drifting into order, converging into a singularity / event horizon”.
- Create an animated background that resembles the provided concept art (navy space-like field, scattered data points on the left that become ordered streams toward the right, ending in an orange glowing singularity with rings).
- The background must be subtle and not harm readability (use overlay gradients, opacity, blur, and z-index layering).
- Implement the animation with Canvas/WebGL if reasonable; otherwise use CSS + canvas. Keep it performant and accessible.
- Respect reduced motion: if prefers-reduced-motion is enabled, reduce animation intensity or disable.

## THE SITE MUST INCLUDE THESE SECTIONS / PAGES

1. Landing / Home (hero)
   - Immediately communicates “Data Engineering & Software Engineering” + cloud/data platforms + architecture mindset.
   - Hero content: short punchy intro, 2–3 key bullets, and 2 CTAs: “View Projects” and “Contact”.
   - Include a “capabilities snapshot” (cards) below the hero.

2. Services (Leistungen)
   - A structured list of offerings. Keep it credible and non-marketing-hype.
   - Suggested categories:
     - Cloud Data Platforms (Azure/Fabric/Databricks)
     - Data Warehousing & Dimensional Modeling (Single Source of Truth)
     - ETL/ELT Pipelines, Data Quality, Governance
     - Analytics & BI (Power BI / semantic models)
     - Backend APIs & Integration (TypeScript/NestJS, Python/FastAPI)
     - Architecture & Tech Leadership (standards, CI/CD, testing, roadmaps)
   - Each service has: what it is, typical outcomes, relevant tech keywords.

3. Blog
   - A blog listing with the ability to group posts (categories/tags).
   - Implement: blog list page, blog detail page, tag/category filtering UI.
   - Content source: start with local markdown files in the repo (e.g., /content/blog/\*.md) with frontmatter (title, date, tags, summary, readingTime, slug).
   - Implement a small build-time or runtime loader. Prefer static import via Vite/Angular builder approach if feasible; otherwise use a JSON index generated by a script.
   - Provide sample blog posts (3–5) relevant to data engineering/software engineering.

4. Projects
   - A portfolio list with project detail pages.
   - Projects can be stored as markdown or JSON (similar to blog) under /content/projects.
   - Each project: title, client/industry (if anonymized), problem, approach, architecture, tech stack, results, responsibilities.
   - Provide 3–6 sample projects aligned with:
     - Azure Data Platform / Databricks / Power BI / Unity Catalog
     - Lakehouse + DWH
     - ETL/ELT, data validation, monitoring
     - Backend APIs (NestJS/Redis/BullMQ) and/or Python pipelines
   - Use an “engineering case study” style: problem → constraints → design → implementation → outcomes.

5. Contact
   - Contact form with validation (name, email, message, optional subject).
   - Provide a safe implementation approach:
     - Either use a simple serverless endpoint placeholder (document how to wire it) OR
     - Use email-to link fallback, but still implement the UI form.
   - Add spam prevention approach (honeypot field + rate limiting notes).
   - Show status: sending, success, error.

6. Socials
   - Prominent icons/links in footer and optionally on contact/about:
     - LinkedIn
     - freelancermap.de
     - freelance.de
   - Open in new tab with rel="noopener".

### OPTIONAL (IF TIME)

- About section (short bio + values: structured, pragmatic, maintainable, partner-minded).
- “Tech Stack” section with badges/chips.
- Basic SEO: meta tags, OpenGraph, sitemap stub.
- Lightweight theming system consistent with Spartan UI.

## TECH REQUIREMENTS

- Angular (latest in repo) + Spartan UI. Keep the design system consistent.
- Clean architecture:
  - /app/core (layout, shell, services)
  - /app/shared (ui components)
  - /app/features (home, services, blog, projects, contact)
  - routing with lazy-loaded feature modules or standalone routes
- Strong TypeScript typing.
- Use Angular Signals where it makes sense (state, filtering), but don’t overcomplicate.
- Responsiveness: mobile-first; ensure text remains readable over animated background.
- Accessibility: focus states, keyboard navigation, semantic headings, aria labels, reduced motion, contrast.
- Performance: avoid heavy dependencies. Keep animation CPU/GPU friendly.
- Testing: at least basic unit tests for content parsing + filtering logic; optionally e2e if easy.
- Documentation:
  - README: how to run, how to add blog posts/projects, how to configure contact endpoint.
  - Inline code comments where logic is non-trivial.

## ANIMATED BACKGROUND SPEC (IMPORTANT)

Implement a reusable component like <app-singularity-background>.

- Visual behavior:
  - Left side: many faint points (data) drifting slowly.
  - Mid: points increasingly align into curved streams (like gravitational field lines).
  - Right side: orange singularity glow + concentric rings (subtle) + a vertical “data jet” effect.
  - Keep it subtle: the UI content must remain primary.
- Technical behavior:
  - Canvas with requestAnimationFrame.
  - Resize handling (devicePixelRatio aware).
  - Configurable intensity via inputs.
  - Pauses or reduces intensity on reduced motion.
  - Do not block main thread; keep per-frame computations lightweight.
- Integrate with page layout so the background sits behind content with an overlay gradient for readability.

## CONTENT TONE

- No exaggerated marketing (“world-class”, “revolutionary”). Use precise, engineering language.
- Show competence through structure and clarity.

## DELIVERABLES

- Fully working Angular app with routes:
  / (home)
  /services
  /blog
  /blog/:slug
  /projects
  /projects/:slug
  /contact
- Implement navigation header + footer.
- Provide sample content for blog/projects.
- Provide a clean theme consistent with navy/orange brand.
- Ensure build passes and the site runs locally.

## WORKFLOW

- Make incremental commits with clear messages:
  1. scaffold + routing + layout
  2. theming + Spartan UI integration
  3. background animation component
  4. content system (blog/projects)
  5. pages + UI polish
  6. contact form
  7. tests + docs

## CONSTRAINTS / PITFALLS TO AVOID

- Do not make it look like a generic template.
- Do not overuse gradients/glows; keep it technical and restrained.
- Do not put important text under the left-side profile-photo zone for social banners (not relevant to website, but follow “safe composition” thinking).
- Keep dependencies minimal.

Start by inspecting the repository, identifying Angular version/tooling, and then implement the structure above. If something is missing (e.g., Angular not initialized), initialize it with best-practice defaults and integrate Spartan UI accordingly.
